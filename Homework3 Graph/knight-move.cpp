#include <iostream>
#include <string>

using namespace std;

void find_way(int col, int row, int direction)
{
    if(col <= 0 && row <= 0)return;
}

int main()
{
    int N;
    cin >> N;
    for (int i = 0; i< N; ++i)
    {
        string s1,s2;
        cin >> s1 >> s2;
        int col = s2[0] - s1[0];
        int row = s2[1] - s1[1];
        int counter = 0;
        if ()

    }
}